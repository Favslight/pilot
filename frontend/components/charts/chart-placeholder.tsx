import { Card } from "@/components/ui/card";

export function ChartPlaceholder() {
  return <Card className="flex h-64 items-center justify-center text-sm text-slate-500">Reporting charts will be connected in a later phase.</Card>;
}
